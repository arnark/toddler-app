import { colors } from './colors';

const newButton = {

  height: 60,
  flexDirection: 'row',
  backgroundColor: '#2e2c2e',
  justifyContent: 'center',
  alignItems: 'center'
};

export default newButton;
